{
  "name": "claude-cockpit",
  "displayName": "Claude Cockpit",
  "description": "Surface Claude Code's hidden state — token burn, files touched, memory, modes — directly in VSCode.",
  "version": "0.16.0",
  "publisher": "dashable",
  "license": "MIT",
  "repository": {
    "type": "git",
    "url": "https://github.com/sboghossian/claude-cockpit.git"
  },
  "homepage": "https://claude-cockpit.pages.dev",
  "engines": {
    "vscode": "^1.85.0"
  },
  "categories": [
    "Other",
    "Visualization"
  ],
  "keywords": [
    "claude",
    "claude-code",
    "anthropic",
    "ai",
    "cockpit"
  ],
  "main": "./out/extension.js",
  "activationEvents": [
    "onStartupFinished"
  ],
  "contributes": {
    "viewsContainers": {
      "activitybar": [
        {
          "id": "claudeCockpit",
          "title": "Claude Cockpit",
          "icon": "media/icon.svg"
        }
      ]
    },
    "views": {
      "claudeCockpit": [
        {
          "id": "claudeCockpit.sidebar",
          "name": "Cockpit",
          "type": "webview"
        }
      ]
    },
    "commands": [
      {
        "command": "claudeCockpit.refresh",
        "title": "Claude Cockpit: Refresh",
        "icon": "$(refresh)"
      },
      {
        "command": "claudeCockpit.openMemory",
        "title": "Claude Cockpit: Open MEMORY.md"
      },
      {
        "command": "claudeCockpit.openSessionFile",
        "title": "Claude Cockpit: Open Active Session JSONL"
      },
      {
        "command": "claudeCockpit.saveToObsidian",
        "title": "Claude Cockpit: Save Session to Obsidian"
      },
      {
        "command": "claudeCockpit.openVault",
        "title": "Claude Cockpit: Open Obsidian Vault"
      },
      {
        "command": "claudeCockpit.searchAllSessions",
        "title": "Claude Cockpit: Search All Sessions"
      },
      {
        "command": "claudeCockpit.watchtower",
        "title": "Claude Cockpit: Watchtower (cross-project sessions)"
      },
      {
        "command": "claudeCockpit.setDailyCap",
        "title": "Claude Cockpit: Set Daily Budget Cap"
      }
    ],
    "configuration": {
      "title": "Claude Cockpit",
      "properties": {
        "claudeCockpit.budget.enabled": {
          "type": "boolean",
          "default": false,
          "description": "Enable budget alerts when daily/session caps are exceeded."
        },
        "claudeCockpit.budget.dailyCapUsd": {
          "type": "number",
          "default": 0,
          "minimum": 0,
          "description": "Daily spend cap in USD across all projects. 0 disables the daily cap."
        },
        "claudeCockpit.budget.sessionCapUsd": {
          "type": "number",
          "default": 0,
          "minimum": 0,
          "description": "Per-session spend cap in USD. 0 disables the session cap."
        },
        "claudeCockpit.cloudRoutines.enabled": {
          "type": "boolean",
          "default": false,
          "description": "Surface a deep-link to manage cloud routines (scheduled remote agents) on claude.ai. Off by default to preserve Cockpit's local-only stance — turning this on does not yet make any network calls; it just unhides the link."
        },
        "claudeCockpit.theme": {
          "type": "string",
          "enum": ["auto", "dark", "light"],
          "default": "auto",
          "description": "Initial theme for the Cockpit sidebar. 'auto' inherits VSCode's theme via CSS variables (default). 'dark'/'light' force a fixed palette regardless of VSCode's theme. The Customize panel inside the sidebar can override this at runtime."
        },
        "claudeCockpit.discover.enabled": {
          "type": "boolean",
          "default": false,
          "description": "Enable the Discover tab (top GitHub projects + RSS from Obsidian). Off by default — turning it on lets Cockpit fetch api.github.com on demand (when you click Refresh). RSS reads from your Obsidian vault and makes no network calls."
        },
        "claudeCockpit.wisprShortcut": {
          "type": "string",
          "default": "",
          "description": "Optional. Your Wispr Flow keyboard shortcut so Cockpit's Talk tab can trigger dictation. Format: 'control+option+space'. Empty = no synthetic trigger; you press the shortcut yourself. macOS only."
        },
        "claudeCockpit.updateCheck.enabled": {
          "type": "boolean",
          "default": true,
          "description": "Periodically check api.github.com/repos/sboghossian/claude-cockpit/releases/latest for new releases of this extension and surface an 'Update available' pill in the header. Set to false for fully local operation with zero update-check traffic."
        },
        "claudeCockpit.roadmap.enabled": {
          "type": "boolean",
          "default": true,
          "description": "Allow the Roadmap tab to fetch project metadata from roadmap.dashable.dev (or localhost:3000 if running the roadmap server locally). On by default — the endpoint is owned by the same operator as this extension. Set to false to make the Roadmap tab show only cached data (under ~/.claude/.cache/cockpit-roadmap.json) with no outbound traffic."
        },
        "claudeCockpit.surfaces.macHealth": {
          "type": "boolean",
          "default": true,
          "description": "Show the Mac Health tab (disk / memory / battery / CPU / Wi-Fi / Bluetooth peripherals). Disable to hide the tab and skip the periodic system_profiler probe (~1s every 30s). Cockpit's wedge is Claude observability, not iStat — keep this off if you already use a dedicated system monitor."
        },
        "claudeCockpit.surfaces.appUsage": {
          "type": "boolean",
          "default": true,
          "description": "Track which macOS app is in focus once per minute (cheap, local-only) and render today's hourly bar chart. Disable to skip the lsappinfo poll and hide the App Usage panel."
        },
        "claudeCockpit.surfaces.subdomainHealth": {
          "type": "boolean",
          "default": true,
          "description": "HEAD-probe the always-live subdomains in your PILOT memory every 60s so the dots reflect reality. Disable for fully local operation with zero outbound HEAD requests."
        }
      }
    },
    "menus": {
      "view/title": [
        {
          "command": "claudeCockpit.refresh",
          "when": "view == claudeCockpit.sidebar",
          "group": "navigation"
        }
      ]
    }
  },
  "scripts": {
    "compile": "tsc -p .",
    "watch": "tsc -p . --watch",
    "test": "npm run compile && node --require ./test/register.js --test test/claudeData.test.js",
    "vscode:prepublish": "npm run compile"
  },
  "devDependencies": {
    "@types/node": "^20.0.0",
    "@types/vscode": "^1.85.0",
    "typescript": "^5.4.0"
  }
}
